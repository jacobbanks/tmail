package main

// Version information
var (
	// Version is the current version of tmail
	Version = "0.1.0"
	
	// BuildDate is the date when the binary was built
	BuildDate = "unknown"
	
	// GitCommit is the git commit hash
	GitCommit = "unknown"
	
	// GitState is the git state (clean or dirty)
	GitState = "unknown"
)